import { PayoneerIcon } from "untitledui-vite-starter-kit";

export const Default = () => <PayoneerIcon className="h-10 w-auto" />;

export const Sizes = () => (
    <div className="flex flex-wrap items-end gap-6">
        <PayoneerIcon className="h-6 w-auto" />
        <PayoneerIcon className="h-8 w-auto" />
        <PayoneerIcon className="h-10 w-auto" />
        <PayoneerIcon className="h-14 w-auto" />
    </div>
);
