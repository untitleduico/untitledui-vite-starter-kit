import { YandexIcon } from "untitledui-vite-starter-kit";

export const Default = () => <YandexIcon className="h-10 w-auto" />;

export const Sizes = () => (
    <div className="flex flex-wrap items-end gap-6">
        <YandexIcon className="h-6 w-auto" />
        <YandexIcon className="h-8 w-auto" />
        <YandexIcon className="h-10 w-auto" />
        <YandexIcon className="h-14 w-auto" />
    </div>
);
